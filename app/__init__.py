# This file makes 'app' a Python package.
# You can define package-level imports or initializations here if needed.
# For a typical FastAPI structure, this can often be kept minimal or empty,
# as the main application object is usually created in app.main.
